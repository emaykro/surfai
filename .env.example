# Database
DATABASE_URL=postgresql://localhost:5432/surfai

# Server
PORT=3000

# Security — operator API auth (required in production)
OPERATOR_API_TOKEN=change-me-to-a-random-secret

# Set to "true" only for local dev without siteKey setup
# ALLOW_INGEST_WITHOUT_SITEKEY=true
